// MoveMentor · MoonBit 动作安全翻译引擎
// 参赛模块名：hackathon/movementor
// 发布到 mooncakes.io 前请把 name 改成 <你的 GitHub 用户名>/movementor。

name = "hackathon/movementor"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "health", "accessibility", "pose", "dsl", "rule-engine" ]

preferred_target = "js"

description = "把年轻人健身动作翻译成低冲击变式的规则引擎：DSL 规则表 + 约束校验 + 计数状态机，核心逻辑 100% 用 MoonBit 编写。"
